import styled from 'styled-components';

const StyledLine = styled.div`
  width: 100%;
  height: 3px;
  background: linear-gradient(0deg, #ffffff, hsla(0, 0%, 50%, 1));
  opacity: 0.7;
  border-radius: 0px 3px 3px 0px;
`;

export default StyledLine;
